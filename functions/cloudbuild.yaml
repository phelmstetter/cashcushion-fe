steps:
  # 1. Install dependencies using the lock file for a reproducible build
  - name: 'node:20'
    id: 'Install'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "Cleaning old modules..."
        rm -rf node_modules
        echo "Installing dependencies..."
        npm install
    dir: 'functions'

  # 2. Deploy Gen2 gateway function
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy'
    entrypoint: 'gcloud'
    args:
      - 'functions'
      - 'deploy'
      - 'gateway'
      - '--gen2'
      - '--region=us-central1'
      - '--runtime=nodejs20'
      - '--source=.'
      - '--entry-point=gateway'
      - '--trigger-http'
      - '--project=cashcushion'
    dir: 'functions'
    waitFor: ['Install']

timeout: '1600s'
options:
  logging: CLOUD_LOGGING_ONLY
