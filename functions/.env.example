# Comma-separated, exact HTTPS URLs registered in Plaid Dashboard > Developers > API.
# Do not use wildcards or local HTTP addresses in a deployed environment.
PLAID_ALLOWED_REDIRECT_URIS=https://your-app.web.app/linked-accounts